{
    "rules": [
      {
        "conditions": [
          {"field": "From", "predicate": "contains", "value": "example.com"},
          {"field": "Subject", "predicate": "contains", "value": "example"},
          {"field": "Received Date", "predicate": "less than", "value": "2"}
        ],
        "predicate": "All",
        "actions": ["Mark as Read"]
      }
    ]
  }
